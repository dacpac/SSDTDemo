<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    <PropertyGroup Label="Common">
        <Environment/>
        <RootDir>$(MSBuildProjectDirectory)</RootDir>
        <BuildOutputDir>$([System.IO.Path]::GetFullPath('$(RootDir)\..\Build'))</BuildOutputDir>
        <SourceCodeDir>$([System.IO.Path]::GetFullPath('$(RootDir)\..\src'))</SourceCodeDir>
        <DeployToDatabase>+</DeployToDatabase>
        <MessageSymbols>**********</MessageSymbols>
        <Configuration>Debug</Configuration>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'Local' ">
        <ServerDataSource>localhost</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'Dev' ">
        <ServerDataSource>DEV-DISQLSRV01</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'WIP' ">
        <ServerDataSource>DEV-DIWIPSRV01</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'RC' ">
        <ServerDataSource>RC-DISQL01</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'Live' ">
        <ServerDataSource>DISQLSRV01</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
        <DeployToDatabase>-</DeployToDatabase>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'MIG_WIP' ">
        <ServerDataSource>DEV-MIGDBSRV02</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'MIG_RC' ">
        <ServerDataSource>MIG-INT-V2.preprod.wonga.global</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'MIG_PREPROD' ">
        <ServerDataSource>UK-PREPROD-V3DB.preprod.wonga.global</ServerDataSource>
        <PullLiveDailyTargetServer>UK-PREPROD-V2DB.preprod.wonga.global</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>UK-PREPROD-V2DB.preprod.wonga.global</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Environment)' == 'MIG_LIVE' ">
        <ServerDataSource>DB02.wonga.global</ServerDataSource>
        <PullLiveDailyTargetServer>$(ServerDataSource)</PullLiveDailyTargetServer>
        <PullLiveDataForCubeTargetServer>$(ServerDataSource)</PullLiveDataForCubeTargetServer>
    </PropertyGroup>

</Project>